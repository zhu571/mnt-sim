# ImQMD 规范 vs 代码 差距分析报告

**生成日期**: 2026-06-27  
**规范文件**: `papers/imqmd_model_spec.md` (1026 行, 9 节 + 1 检查清单)  
**代码文件**: `mnt_sim/imqmd/` (7 个 Python 文件: `__init__.py`, `nucleus.py`, `initializer.py`, `propagator.py`, `skyrme.py`, `collisions.py`, `fragments.py`, `decay.py`)  
**分析范围**: 逐项对照规范 9 节内容，标记 ✓(已实现) / ✗(缺失) / ⚠(部分实现或有偏差)

---

## 图例

| 标记 | 含义 |
|------|------|
| ✓ | 已实现，与规范一致 |
| ⚠ | 部分实现或存在偏差 |
| ✗ | 完全缺失 |

---

## 1. 高斯波包表示 (Gaussian Wave Packet Representation)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 1.1 | 相干高斯波包函数 (W2002-1): ϕ_i(r) 含指数项 exp[-(r-r_i)²/4σ_r² + i/ħ r·p_i] | ⚠ | 中 | `nucleus.py:GaussianPacket` 存储 r_i, p_i, sigma_r, is_proton，但**未显式实现波包函数**。密度计算使用实空间高斯重叠，等价于 |ϕ|²。未包含虚部相位因子。 | 添加 `gaussian_wave_packet(r, r_i, p_i, sigma_r)` 函数实现 W2002-1。 |
| 1.2 | 多体试探态为直积 Φ=∏ϕ_i，反对称化未显式处理 | ✓ | — | 代码以列表存储独立 packet，未显式反对称化，符合规范。 | — |
| 1.3 | 单体 Wigner 分布 (W2002-2): f_i(r,p) | ✗ | 低 | `collisions.py:_occupation_wigner()` 内部计算了 Wigner 分布用于 Pauli 阻塞，但**未暴露为独立可调用函数**。 | 添加 `wigner_distribution(r, p, r_i, p_i, sigma_r)` 公开函数。 |
| 1.4 | 坐标密度 ρ_i(r) (W2002-5) 和动量密度 g_i(p) (W2002-6) | ⚠ | 低 | `nucleus.py:density()` 正确计算了 ρ_n(r), ρ_p(r)，归一化因子为 1/(2πσ_r²)^(3/2)，与 W2002-5 一致。但**动量密度 g_i(p) 未实现**。 | 添加 `momentum_density(p, p_i, sigma_r)` 函数。 |
| 1.5 | 不确定性关系 σ_r σ_p = ℏ/2 (W2002-7) | ⚠ | 中 | `collisions.py:_occupation_wigner()` 中使用了 `sigma_p = HBAR_C/(2*sigma_r)`，但**未在初始化或传播中显式约束**此关系。`skyrme.py:HBAR_C=197.327` 正确。 | 在 `GaussianPacket` 中自动计算并存储 sigma_p；在初始化时验证不确定性关系。 |
| 1.6 | IQ1 宽度公式: σ_r = 0.16 A_N^(1/3) + 0.49 fm (W2002-18) | ⚠ | 高 | `skyrme.py:PARAMETER_SETS` 存储了 σ₀, σ₁ 参数对。但**代码中未根据 A 自动计算宽度**；`nucleus.py:sigma_r` 只返回第一个 packet 的 sigma_r，初始化时传入固定值。 | 实现 `compute_sigma_r(A, params)` 函数，在初始化和反应初始化时自动调用。 |
| 1.7 | 通用宽度形式 σ_r(A) = σ₀ + σ₁ A^(1/3) | ⚠ | 高 | 同上。参数已定义但未被使用。 | 同上。 |

**1 节总评**: 核心密度计算正确，但波包函数、Wigner 分布、动量密度未暴露为独立 API；宽度公式参数已定义但未根据系统尺寸自动应用。**严重度最高的问题**是宽度未随 A 动态调整。

---

## 2. 哈密顿量 (Hamiltonian)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 2.1 | 哈密顿方程: ṙ_i = ∂H/∂p_i, ṗ_i = -∂H/∂r_i (W2014-2) | ✓ | — | `propagator.py:_derivatives()` 返回 drdt = p/m, dpdt = forces。`_rk4_step()` 使用经典 RK4 积分。 | — |
| 2.2 | 动能 T = Σ p_i²/(2m) (W2002-9,10) | ✓ | — | `skyrme.py:kinetic_energy()` 计算 Σp²/(2m)，M_N=938.9 MeV。 | — |
| 2.3 | 总势能 U = U_loc + U_Coul (W2014-3,4) | ⚠ | 中 | `nucleus.py:energy_components()` 分离了 kinetic/skyrme/surface/coulomb/symmetry/static。但 `skyrme_potential()` 包含了本该属于 U_loc 的 2-body、3-body 和 g_τ 项，而 surface 被单独分离为有限力程凝聚项（非 (∇ρ)² 形式）。 | 重构 EDF 计算，使 U_loc 精确对应 W2014-5 的项结构。 |
| 2.4 | 现代 Skyrme EDF 二体项: α/2·ρ²/ρ₀ (W2014-5) | ✓ | — | `skyrme.py:skyrme_potential()`: `p.alpha/(2.0*p.rho0) * Σρ`。与规范一致。 | — |
| 2.5 | 密度相关项: β/(γ+1)·ρ^(γ+1)/ρ₀^γ (W2014-5) | ⚠ | 中 | `skyrme_potential()`: `p.beta/((p.gamma+1.0)*p.rho0**p.gamma) * Σρ^p.gamma`。规范为对 ρ^(γ+1) 积分，代码对采样点 ρ^γ 求和。**积分形式不同但采样近似可接受**。 | 确认采样近似在典型核子数下等价于体积积分。 |
| 2.6 | 同位旋标量表面项: g_sur/(2ρ₀)·(∇ρ)² (W2014-5) | ✗ | 高 | `skyrme.py:surface_pair_energy()` 使用**有限力程高斯凝聚项** `-gsur/7 * Σexp(-r_ij²/4σ²)`，而非规范的 (∇ρ)² 项。这是完全不同的物理机制。 | 实现基于密度梯度的 (∇ρ)² 表面项，或明确文档说明有限力程替代方案的等价性。 |
| 2.7 | Thomas-Fermi 项: g_τ·ρ^(η+1)/ρ₀^η (W2014-5) | ✓ | — | `skyrme_potential()`: `p.g_tau/(p.rho0**p.eta) * Σρ^p.eta`。 | — |
| 2.8 | 对称能+表面对称能: C_s/(2ρ₀)·[ρ²-κ_s(∇ρ)²]·δ² (W2014-5) | ⚠ | 高 | `skyrme.py:symmetry_energy()` 实现了 `c_sym/(2ρ₀)·Σρ·δ²`，即 ρ²δ²/ρ₀ 部分。**但完全缺失 κ_s(∇ρ)²δ² 表面对称能项**。参数 κ_s 已在 `SkyrmeParameters` 中定义但未使用。 | 添加表面-对称能贡献，使用密度梯度计算。 |
| 2.9 | 冷核物质对称能 E_sym(ρ) (W2014-6) | ✗ | 低 | 未实现。此公式用于验证参数集。 | 添加 `symmetry_energy_nm(rho)` 辅助函数。 |
| 2.10 | 斜率参数 L (W2014-7) | ✗ | 低 | 未实现。 | 添加 `slope_parameter_L()` 辅助函数。 |
| 2.11 | 原始 2002 EDF (W2002-13) | ✗ | 中 | `PARAMETER_SETS` 未包含 Wang 2002 原始参数 (-124, 71, g₀=960, C_s=32)。且原始 EDF 使用 (ρ_p-ρ_n)²/ρ₀ 形式的对称能，不同于现代形式的 δ²。 | 如需支持原始参数，添加一个 EDF 模式选择器，或添加 Wang2002ParameterSet。 |
| 2.12 | ImQMD05 显式动量相关相互作用 u_md (Z2012-5) | ✗ | 高 | 完全未实现。无 `u_md` 积分、无 `Δp` 相关项、无修正的运动方程 `ṙ_i = p_i/m + ∂U_md/∂p_i`。 | 对于中能应用，需实现动量相关相互作用和对应的运动方程修正。 |
| 2.13 | 直接库仑能 (W2002-17) | ✓ | — | `skyrme.py:coulomb_energy()` 使用 `e²·erf(r/2σ)/r` 形式，正确处理了高斯电荷分布。 | — |
| 2.14 | Slater 交换库仑项 (Zhao2016-2) | ✗ | 高 | `coulomb_energy()` 仅有直接项，**缺失 Slater 交换项** `-(3/4)e²(3/π)^(1/3)∫ρ_p^(4/3)d³R`。对 MNT/熔合计算重要。 | 添加 Slater 交换库仑项，在网格上积分 ρ_p^(4/3)。 |
| 2.15 | 密度及梯度从波包计算 (density, grad-density) | ⚠ | 低 | `nucleus.py:density()` 计算了高斯重叠密度。但梯度 ρ 用于表面项的 (∇ρ)² **未被计算**（因表面项使用有限力程替代方案）。 | 如需实现规范表面项，需添加 `density_gradient()` 函数。 |
| 2.16 | 网格积分 vs 解析高斯折叠 | ⚠ | 中 | 代码使用**质心采样**（在核子位置评估密度），而非网格积分。对于静态核诊断可接受，但对于 (∇ρ)² 等项需要网格。 | 评估是否需要在网格上实现 EDF 积分以获得更高精度。 |

**2 节总评**: 动能、二体项、密度相关项、Coulomb 直接项正确实现。**严重缺陷**: 表面项使用有限力程凝聚替代 (∇ρ)²；表面对称能项 κ_s(∇ρ)²δ² 完全缺失（κ_s 参数定义但未使用）；Slater 交换库仑项缺失；ImQMD05 动量相关相互作用缺失。EDF 当前为**简化变体**，非规范严格实现。

---

## 3. 基态初始化 (Ground State Initialization)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 3.1 | 从 RMF 密度分布采样 (原始 2002) | ✗ | 低 | 未实现。代码使用硬球采样。RMF 密度读取仅在早期 ImQMD 中使用，现代版本多用硬球+中子皮修正。 | 如仅支持现代版本可忽略。 |
| 3.2 | 硬球初始化+中子皮修正: R_c 公式 (W2014-8) | ✗ | 高 | `initializer.py:initialize_nucleus()` 使用 `radius = 1.14*A^(1/3)` 的简单硬球，**未使用规范的中子皮修正公式** W2014-8, W2014-9, init-rp, init-rn。 | 实现完整的中子皮初始化链: R_c → ⟨r_c²⟩ → R_p → R_n → 分中子/质子采样。 |
| 3.3 | 中子皮公式 ΔR_np = 0.9I - 0.03 (W2014-9) | ✗ | 高 | 未实现。中子/质子使用相同半径和混洗后的位置。 | 同上。 |
| 3.4 | 采样半径修正 w_r = 0.8 fm (init-pos) | ✗ | 高 | 代码使用 `min_dist=0.85` 的最小间距，但**未从半径中减去 w_r=0.8**。 | 在采样半径 R_p, R_n 上减去 w_r。 |
| 3.5 | 局域费米动量: p_F,q = ℏ[3π²ρ_q]^(1/3) - w_p (init-pf) | ⚠ | 高 | `_fermi_momenta()` 计算了 `p_f = HBAR_C*(3π²ρ_q)^(1/3)`，但**未减去 w_p 修正**。动量随后乘以 scale (0.50-0.70)，这是一个经验替代。 | 实现完整的 w_p 修正，通过与实验结合能的迭代确定 w_p。 |
| 3.6 | 结合能接受条件: E_ground ∈ BE±0.05 MeV (init-energy) | ✗ | 高 | 代码使用 `empirical_binding_per_nucleon()` 计算半经验结合能，然后通过 `energy_offset` 校准到目标值。**未进行 BE±0.05 MeV 的严格筛选**，也未使用 Skyrme EDF 自身的基态能。 | 实现基于 EDF 自洽计算的基态结合能，并在 ±0.05 MeV 内筛选。 |
| 3.7 | 相空间距离条件: |Δr||Δp| ≥ 255 fm·MeV/c (init-phase) | ⚠ | 高 | `_phase_space_minimum()` 使用阈值 **170.0 fm·MeV/c**，规范要求 **255 fm·MeV/c**。差异约 33%。 | 将阈值改为 255，或提供参数可配置。 |
| 3.8 | 演化 ≥ 600 fm/c 并检查稳定性 (原始 2002) | ⚠ | 高 | `initialize_nucleus()` 仅演化 50 步 × 1 fm/c = **50 fm/c**。`initialize_and_relax()` 每周期 800 fm/c + 500 fm/c 漂移检查，总计最多 3900 fm/c，但包含阻尼。600 fm/c 无阻尼演化**未执行**。 | 在 `initialize_nucleus()` 中添加 ≥600 fm/c 无阻尼演化，并检查结合能漂移和粒子发射。 |
| 3.9 | 经验均方根半径 (W2002-19): ⟨r²⟩^(1/2) = 0.82A^(1/3)+0.58 | ✗ | 低 | 未用于验证。`nucleus.py:rms_radius()` 正确计算了 rms 半径（含波包宽度修正 3σ_r²），但初始化未用 W2002-19 验证。 | 在初始化后验证 rms 半径。 |
| 3.10 | 传播中的相空间约束 (CoMD 风格) | ✓ | — | `collisions.py:fermi_constraint_check()` 实现了同位旋相同的核子对相空间约束，阈值 140 fm·MeV/c。 | — |
| 3.11 | 质心修正 (cm-correction) | ✓ | — | `initializer.py:_recenter()` 和 `propagator.py:_rk4_step()` 中实现了 position 和 momentum 的质心漂移去除。 | — |
| 3.12 | 现代稳定性要求: 2000 fm/c 时散逸发射 ~1.1 (⁹²Zr) 和 ~2.6 (¹³²Sn) | ⚠ | 中 | `initialize_and_relax()` 通过 `quick_no_spring_drift()` 检查 500 fm/c 的能量漂移，但**未检查粒子发射数**。 | 添加散逸粒子计数检查。 |

**3 节总评**: 初始化使用了**大幅简化的硬球+经验标度**方案，缺少规范要求的中子皮修正 (w_r)、费米动量修正 (w_p)、严格的结合能筛选 (BE±0.05 MeV)、255 fm·MeV/c 相空间阈值、以及 600 fm/c 无阻尼演化。CoMD 约束和 CM 修正正确。**这是当前实现与规范差距最大的部分之一**。

---

## 4. 运动方程 (Equations of Motion)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 4.1 | 哈密顿方程 ṙ_i = ∂H/∂p_i, ṗ_i = -∂H/∂r_i (eom) | ✓ | — | `propagator.py:_derivatives()` 正确实现了 drdt = p/m, dpdt = forces。 | — |
| 4.2 | 力为 EDF 对高斯质心的梯度 (force) | ⚠ | 中 | `_centroid_density_force()` 解析计算了体项对质心的梯度。但表面项使用有限力程力（`_surface_pair_force()`），不直接对应 (∇ρ)² 的梯度。 | 表面项如改为 (∇ρ)² 形式，需重新推导梯度。 |
| 4.3 | 动量相关相互作用的修正方程 (mdi-eom) | ✗ | 高 | 无 ṙ_i = p_i/m + ∂U_md/∂p_i 的实现。当前代码为 `drdt = momenta / M_N`。 | 如需 ImQMD05 兼容，实现 ∂U_md/∂p_i 项。 |
| 4.4 | RK4 积分 (rk4) | ✓ | — | `_rk4_step()` 实现了标准四阶 Runge-Kutta。 | — |
| 4.5 | 时间步长 Δt = 1 fm/c | ✓ | — | 代码默认 dt=1.0。 | — |
| 4.6 | 能量守恒: RK4 需保证论文级稳定性 | ⚠ | 中 | `propagate()` 记录了能量历史。`initialize_and_relax()` 检查了 8% 漂移阈值。但规范要求总能量守恒精度满足千 fm/c 稳定性。当前代码包含**非物理的静态参考场** (`use_static_reference`) 和**动量阻尼** (damping=0.93)，这些会人为改善表观稳定性但非物理。 | 移除阻尼和静态场后验证无约束演化的能量守恒。 |

**4 节总评**: RK4 实现正确，运动方程基本符合哈密顿形式。**缺失**: 动量相关相互作用的修正运动方程。

---

## 5. 核子-核子碰撞 (Nucleon-Nucleon Collisions)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 5.1 | 几何碰撞判据: d_min ≤ √(σ_NN/π) (collision-geom) | ✓ | — | `attempt_nn_collision()` 计算 t_closest 和 d_min，与 d_collision = √(σ·0.1/π) 比较。 | — |
| 5.2 | 最接近距离计算 (closest, dmin) | ✓ | — | 使用 `-r_rel·v_rel/v²` 投影到 [0, dt]，正确处理了接近/远离方向。 | — |
| 5.3 | 自由 NN 截面: Cugnon 参数化 (cugnon-pp, cugnon-np) | ✓ | — | `free_nn_cross_section()` 实现了 pp/nn 和 np 的 Cugnon 公式。β² 计算和低能截断 (60/180 mb) 正确。 | — |
| 5.4 | 低能截断: σ_nn/pp=60 mb, σ_np=180 mb (low-energy-cap) | ✓ | — | E_lab < 50 MeV 时返回 60 或 180 mb。 | — |
| 5.5 | 单位转换: 1 mb = 0.1 fm² | ✓ | — | `MB_TO_FM2 = 0.1`。 | — |
| 5.6 | 介质修正: σ* = (1 - ξρ/ρ₀)σ_free (Z2012-medium) | ✓ | — | `in_medium_factor()` 和 `in_medium_nn_cross_section()` 正确实现，默认 η=0.2。 | — |
| 5.7 | 三种介质修正模式 (case1, case2, case3) | ⚠ | 低 | 仅实现了 case2（同位旋相关抑制）。case1 (无介质修正) 可通过 η=0 实现。case3 (同位旋无关) 未实现。 | 添加 case3 参数化选项（对 pp/nn/np 统一使用平均截面和抑制因子）。 |
| 5.8 | Chen 2024 通用形式 σ_med = (1+η·ρ/ρ₀)σ_free | ⚠ | 低 | 当前使用 `1 - η·ρ/ρ₀` 形式（η 正值表示抑制）。Chen 2024 的 `1 + η·ρ/ρ₀` 允许 η 为正或负。**符号约定不同**。 | 明确文档说明 η 的符号约定，并支持 Chen 2024 的通用形式。 |
| 5.9 | Pauli 阻塞概率: P_block = 1-(1-P_i)(1-P_j) (pauli-block) | ✓ | — | `pauli_blocking_probability()` 和 `_pauli_blocking_probability_fast()` 正确实现。 | — |
| 5.10 | 常规 QMD 占位数 P_i (Chen2024-1) | ✓ | — | `_occupation_wigner()` 使用 2×Σexp(-dr²/2σ_r² - dp²/2σ_p²) 计算。因子 2 考虑了自旋简并。 | — |
| 5.11 | 硬球重叠替代方案 (Chen2024-2) | ✗ | 低 | 未实现。 | 如需对比研究可添加。 |
| 5.12 | Chen 2024 改进 PB(W*) : 多动量态采样 (Chen2024-6,7) | ✗ | 高 | `pauli_blocking_probability_v2()` 仅将 Wigner 核宽度放大 1.35 倍，**未实现 Chen 2024 的多态采样** c_j(p') = (1/N)Σexp[-(p'-P_j,λ)²/2σ_p²]。这是 PB(W*) 的核心创新。 | 实现真正的 PB(W*): 对每个核子 j 生成 N 个动量样本 {P_j,λ}，计算平均占位数。 |
| 5.13 | PB(W*) 方差降低 ~70% | ✗ | 中 | 取决于 5.12 的实现。 | — |
| 5.14 | 碰撞步内避免重复计数 | ✓ | — | 使用 `used` 集合追踪已碰撞核子对。 | — |
| 5.15 | 空间网格邻居搜索 | ✓ | — | `_build_grid()` 和 `_cached_neighbor_pairs()` 实现了 5 fm 网格缓存邻居搜索。 | — |

**5 节总评**: 碰撞模块实现最完整的部分。几何判据、Cugnon 截面、介质修正、Pauli 阻塞均正确实现。**主要缺失**: 真正的 Chen 2024 PB(W*) 多动量态采样（当前 v2 仅为宽度放大）。case3 统一抑制和 Chen 2024 通用 η 符号约定为次要问题。

---

## 6. 碎片识别 (Fragment Recognition)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 6.1 | MST 算法: |Δr| ≤ R₀ 且 |Δp| ≤ P₀, 传递性 | ✓ | `minimum_spanning_tree()` 使用连通分量算法正确实现了传递性聚类。 | — |
| 6.2 | 标准 ImQMD05 参数: R₀=3.5 fm, P₀=250 MeV/c (Z2012-MST) | ⚠ | 中 | 默认值 `r_cut=3.0`, `p_cut=300.0`。规范要求 R₀=3.5, P₀=250。差异在 R₀ 相差 ~14%。 | 将默认参数改为 3.5 和 250，或通过 `reaction_fragments()` 的文档说明。 |
| 6.3 | 同位旋相关 MST (iso-MST): R_nn=6, R_np=6, R_pp=3 fm (iso-MST-R) | ✗ | 高 | `isospin_mst()` 存在但**默认值完全不同**: r_cut_pp=2.8, r_cut_nn=2.8, r_cut_np=3.2。规范要求 R_pp=3（质子对截断更小）和 R_nn=R_np=6（中子/混合对截断更大以容纳中子皮/晕）。代码的 np 截断仅略大于 pp/nn。 | 将 iso-MST 默认值改为规范值: R_pp=3.0, R_nn=6.0, R_np=6.0。 |
| 6.4 | iso-MST 动量截断 P₀=250 MeV/c (iso-MST-P) | ⚠ | 低 | 默认 `p_cut=300.0`。 | 改为 250。 |
| 6.5 | 轻复杂粒子表面凝聚 (d, t, ³He, ⁴He) (Wei2014) | ✗ | 高 | `coalescence_light()` 实现了相空间凝聚，但**非 Wei 2014 的表面凝聚机制**。缺失: 核心半径 R₀、表面厚度 D₀、领头核子概念、Coulomb 位垒穿透检查、Jacobi 坐标相空间条件 R_im·P_im ≤ h₀、能量依赖的 h₀ (200/260/330 MeV·fm/c)、LCP 动能公式 (Wei2014-13)。 | 实现 Wei 2014 的完整表面凝聚模型，或明确文档说明当前简化凝聚的适用范围。 |
| 6.6 | 凝聚优先级: ⁴He > ³He > t > d (LCP-priority) | ✓ | — | `coalescence_light():templates` 顺序正确: (2,4), (2,3), (1,3), (1,2)。 | — |
| 6.7 | 自适应 MST 和反应碎片识别 | ⚠ | 低 | `adaptive_mst()` 和 `reaction_fragments()` 是代码特有的增强算法，非规范中描述。规范仅定义了 MST 和 iso-MST。这些增强功能本身有价值，但需明确标注为非标准扩展。 | 在文档中标注为非标准扩展算法。 |

**6 节总评**: MST 基本框架正确。**严重问题**: iso-MST 参数与规范完全不同（R_nn 应为 6 而非 2.8）；表面凝聚模型缺失 Wei 2014 的核心机制。

---

## 7. 退激发 (De-excitation)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 7.1 | 激发能 E* = E_int - E_ground (excitation) | ✓ | — | `compute_fragment_excitation()` 正确移除碎片 CM 运动后计算内能，减去基态结合能。 | — |
| 7.2 | 碎片静止系内能计算 | ✓ | — | `compute_fragment_excitation()` 减去了 CM 动量 `p_cm` 后重新构建子核计算能量。 | — |
| 7.3 | HIVAP 耦合: γ, n, p, α, fission 道 (HIVAP-channels) | ✗ | 高 | 代码未包含与 HIVAP 的接口。`weisskopf_evaporation()` 仅有中子蒸发。 | 设计 HIVAP/GEMINI 耦合接口，接受 (Z,A,E*) 三元组。 |
| 7.4 | Weisskopf-Ewing 蒸发 (Weisskopf) | ✗ | 高 | `weisskopf_evaporation()` 是**大幅简化的中子蒸发模型**，使用温度和相空间因子估计，**未包含**: 逆截面 σ_inv(ε)、子核能级密度 ρ_d、积分计算、分离能和 Coulomb 位垒的完整处理。规范要求完整 Weisskopf-Ewing。 | 重写为完整 Weisskopf-Ewing 形式，包含逆截面积分和能级密度。 |
| 7.5 | 质子、α 等重粒子蒸发道 | ✗ | 高 | 仅有中子蒸发。**未实现** p, α, 及其他 LCP 蒸发道。 | 添加质子、α 及其他 LCP 的蒸发宽度计算。 |
| 7.6 | γ 冷却竞争 | ✗ | 中 | 未实现。 | 添加 γ 宽度作为竞争道。 |
| 7.7 | Bohr-Wheeler 裂变竞争 (Bohr-Wheeler) | ✗ | 高 | `fission_competition()` 是**简化的液滴模型裂变概率**，使用 `exp(-B_f/T) / (exp(-B_f/T) + 1)` 形式。**缺失**: 规范要求的对鞍点态密度 ρ_s 的积分、基态能级密度 ρ_p、裂变位垒 B_f 的完整处理。 | 实现完整 Bohr-Wheeler 积分公式。 |
| 7.8 | 分支比 P_i = Γ_i/Γ_tot (HIVAP-branch) | ⚠ | 中 | `weisskopf_evaporation()` 返回概率分布，但 `fission_competition()` 返回独立概率，**两者未统一在总宽度 Γ_tot 的框架下**。 | 统一所有道在 Γ_tot 框架下计算分支比。 |

**7 节总评**: **当前退激发模块为高度简化版本**。仅有中子蒸发和简化裂变概率，缺失质子/α 蒸发、γ 冷却、HIVAP 耦合、完整的 Weisskopf-Ewing 积分和 Bohr-Wheeler 积分。激发能计算正确。

---

## 8. 反应观测量 (Reaction Observables)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 8.1 | 熔合截面: σ_fus = 2π∫b g_fus db (W2014-10) | ✗ | 高 | 完全缺失。当前代码无碰撞参数循环、无事件分类。 | 实现碰撞参数扫描框架。 |
| 8.2 | 初级碎片截面: σ(Z,A,E*) (Zhao2016-3) | ✗ | 高 | 缺失。 | 同上，结合碎片识别和退激发。 |
| 8.3 | 能谱 dσ/dE (Wei2014-14) | ✗ | 高 | 缺失。 | 实现能谱累积。 |
| 8.4 | 双微分截面 d²σ/dΩdE (Wei2014-15) | ✗ | 高 | 缺失。 | 实现角度和能量双微分截面。 |
| 8.5 | 同位素/质量分布 dσ/dZ, dσ/dA (dZdA) | ✗ | 高 | 缺失。 | 实现分布累积。 |
| 8.6 | 角分布 dσ/dΩ (angular) | ✗ | 高 | 缺失。 | 实现角度分布。 |
| 8.7 | 熔合事件设置: 束流加速 (boost) | ✗ | 高 | 缺失。无初始分离距离 d₀ (30/40 fm)、无 Coulomb 位垒加速。 | 实现双核反应初始化模块。 |
| 8.8 | 熔合判据: 质心距离 < 复合核半径 | ✗ | 高 | 缺失。 | 实现熔合事件分类。 |
| 8.9 | Zhao 2016 U+U 基准设置: E_gs=7.37 MeV/A, β₂=0.215, β₄=0.093, b_max=15, Δb=0.15, d₀=40, N_events=100000 | ✗ | 高 | 完全缺失。 | 实现 U+U 基准工作流。 |

**8 节总评**: **整个反应观测量模块完全缺失**。当前代码仅支持单核静态演化，不具备碰撞参数扫描、熔合事件分类、截面计算等任何反应物理功能。这是 Phase 1（静态核验证）和完整 ImQMD（反应计算）之间的主要差距。

---

## 9. 完整参数表 (Complete Parameter Table)

| # | 规范条目 | 状态 | 严重程度 | 代码位置与说明 | 修复建议 |
|---|---------|------|---------|-------------|---------|
| 9.1 | IQ1 参数集 | ✓ | — | `PARAMETER_SETS["IQ1"]`: α=-310, β=258, γ=7/6, gsur=19.8, g_tau=9.5, η=2/3, c_sym=32, κ_s=0.08, ρ₀=0.165, σ₀=0.49, σ₁=0.16 | — |
| 9.2 | IQ2 参数集 (默认) | ✓ | — | `PARAMETER_SETS["IQ2"]`: α=-356, β=303, γ=7/6, gsur=7.0, g_tau=12.5, η=2/3, c_sym=32, κ_s=0.08, ρ₀=0.165, σ₀=0.88, σ₁=0.09 | — |
| 9.3 | IQ3 参数集 | ✓ | — | `PARAMETER_SETS["IQ3"]`: α=-207, β=138, γ=7/6, gsur=18.0, g_tau=14.0, η=5/3, c_sym=32, κ_s=0.08, ρ₀=0.165, σ₀=0.94, σ₁=0.018 | — |
| 9.4 | IQ3a 参数集 | ✓ | — | `PARAMETER_SETS["IQ3A"]`: α=-207, β=138, γ=7/6, gsur=16.5, g_tau=14.0, η=5/3, c_sym=34, κ_s=0.4, ρ₀=0.165, σ₀=0.94, σ₁=0.020 | 键名 "IQ3A" vs 规范 "IQ3a"——大小写不匹配。 |
| 9.5 | IQ3b 参数集 | ✓ | — | `PARAMETER_SETS["IQ3B"]`: α=-207, β=138, γ=7/6, gsur=18.0, g_tau=14.0, η=5/3, c_sym=34, κ_s=0.6, ρ₀=0.165, σ₀=0.94, σ₁=0.018 | 同上 "IQ3B" vs "IQ3b"。 |
| 9.6 | SkP* 参数集 | ✗ | 中 | 缺失。规范要求: α=-356, β=303, γ=7/6, gsur=19.5, g_tau=13, η=2/3, c_sym=35, κ_s=0.65, ρ₀=0.162, σ₀=0.94, σ₁=0.018 | 添加到 PARAMETER_SETS。 |
| 9.7 | Wang 2002 原始参数: α=-124, β=71, g₀=960, C_s=32 | ✗ | 中 | 缺失。注意 g₀ 使用了不同归一化（单位 GeV fm⁻⁵ vs MeV fm²），不可直接混用。 | 如需支持，添加独立的参数集并文档说明归一化差异。 |
| 9.8 | 宽度公式自动应用 | ✗ | 高 | 参数存储了 σ₀, σ₁，但未根据 A 计算宽度。 | 实现 `compute_width(A, params)` 函数。 |

**9 节总评**: 5 组 IQ 参数集数值正确。**缺失**: SkP*、Wang 2002 原始参数、宽度公式的自动应用。键名大小写不匹配为小问题。

---

## 10. 完整模型行为检查清单 (Expected Complete-Model Behavior Checklist)

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| 10.1 | 波包: 高斯密度和 Wigner 函数匹配 W2002-1 ~ W2002-7 | ⚠ | 密度计算正确，Wigner 函数未暴露，动量密度未实现。 |
| 10.2 | EDF: 可选择的原始/融合/ImQMD05 EDF | ✗ | 仅有现代 IQ EDF 的简化变体（有限力程表面、缺表面对称能、缺 Slater 交换）。原始和 ImQMD05 完全缺失。 |
| 10.3 | Coulomb: 直接有限密度 + Slater 交换 | ⚠ | 直接项正确，Slater 交换缺失。 |
| 10.4 | 初始化: 硬球+中子皮+w_r=0.8+BE±0.05+255 截断 | ✗ | 大幅简化，缺失全部关键约束。 |
| 10.5 | 稳定性: ≥600 fm/c (原始) / ~2000 fm/c (现代) 无散逸 | ⚠ | 包含阻尼和静态场的演化可保持稳定，但无约束自由演化未验证。 |
| 10.6 | 传播: 哈密顿方程+能量守恒 | ⚠ | RK4 正确但包含非物理阻尼/静态场。 |
| 10.7 | 碰撞: 几何调度+Cugnon+介质修正+Pauli 阻塞 | ✓ | 最完整模块。 |
| 10.8 | Pauli 阻塞: PB(W) 和 PB(W*) 均可选 | ⚠ | PB(W) 正确；PB(W*) 仅宽度放大，非真正多态采样。 |
| 10.9 | 碎片: MST(R₀=3.5,P₀=250) + iso-MST(R_nn/np=6,R_pp=3) | ✗ | 参数全部不同。 |
| 10.10 | 退激发: 静止系 E* + HIVAP/GEMINI 耦合 | ✗ | E* 计算正确但退激发高度简化。 |
| 10.11 | 截面: 碰撞参数积分 | ✗ | 完全缺失。 |
| 10.12 | U+U 基准: Zhao 2016 工作流 | ✗ | 完全缺失。 |

---

## 总结统计

| 类别 | ✓ (已实现) | ⚠ (部分) | ✗ (缺失) | 合计 |
|------|-----------|---------|---------|------|
| 1. 高斯波包 | 1 | 5 | 1 | 7 |
| 2. 哈密顿量 | 7 | 5 | 4 | 16 |
| 3. 初始化 | 2 | 4 | 6 | 12 |
| 4. 运动方程 | 3 | 2 | 1 | 6 |
| 5. NN 碰撞 | 9 | 3 | 3 | 15 |
| 6. 碎片识别 | 2 | 3 | 2 | 7 |
| 7. 退激发 | 2 | 1 | 5 | 8 |
| 8. 反应观测量 | 0 | 0 | 9 | 9 |
| 9. 参数表 | 5 | 0 | 3 | 8 |
| **总计** | **31** | **23** | **34** | **88** |

**高严重度问题统计**: 25 项

---

## 优先级修复路线图

### 第一优先级 (Phase 1 → 完整静态核验证)
1. **初始化完善** (§3): 中子皮修正、w_r/w_p、255 阈值、BE±0.05 筛选、600 fm/c 演化
2. **EDF 修正** (§2): 表面项改为 (∇ρ)²、添加表面对称能 κ_s(∇ρ)²δ²、添加 Slater 交换 Coulomb
3. **宽度公式** (§1): 根据 A 自动计算 σ_r

### 第二优先级 (完整反应模拟)
4. **ImQMD05 动量相关相互作用** (§2): u_md 和修正运动方程
5. **反应初始化** (§8): 双核设置、束流加速、碰撞参数框架
6. **截面计算** (§8): 碰撞参数积分、分布累积
7. **iso-MST 参数修正** (§6): 规范参数值
8. **PB(W*) 完整实现** (§5): 多动量态采样

### 第三优先级 (完整退激发链)
9. **完整 Weisskopf-Ewing** (§7): 逆截面积分、p/α 蒸发道
10. **Bohr-Wheeler 裂变** (§7): 完整鞍点积分
11. **HIVAP/GEMINI 耦合** (§7): 外部接口
12. **表面凝聚** (§6): Wei 2014 机制

### 第四优先级 (完备性)
13. SkP* 和 Wang 2002 参数集 (§9)
14. 能谱/双微分截面 (§8)
15. U+U 基准验证 (§8)
